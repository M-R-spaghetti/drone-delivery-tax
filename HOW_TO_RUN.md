#NYSDroneTaxCalculator #LaunchGuide

## 📋 Требования (Prerequisites)

Прежде чем запускать проект, убедитесь, что у вас установлены:
1. **Node.js** (версия 18 или выше).
2. **Docker Desktop** — необходим для запуска базы данных PostgreSQL + PostGIS.
   - *Если Docker не установлен, вы можете [скачать его здесь](https://www.docker.com/products/docker-desktop/).*
   - *После установки убедитесь, что Docker запущен.*

## 🚀 Как запустить проект локально

### 1. Запуск Базы Данных (Docker)
Проект использует Docker для быстрого развёртывания PostgreSQL + PostGIS.
1. Убедитесь, что у вас запущен **Docker Desktop**.
2. В корневой папке проекта (`wdTestTask`) выполните команду:
   ```bash
   docker-compose up -d
   ```
   Это создаст и запустит контейнер `drone_tax_db`.

### 2. Запуск Бэкенда (Server)
Теперь нужно подготовить базу данных и запустить сервер.
1. Перейдите в папку сервера:
   ```bash
   cd server
   ```
2. Установите зависимости (если ещё не сделано):
   ```bash
   npm install
   ```
3. Создайте таблицы (миграции):
   ```bash
   npm run migrate
   ```
4. Наполните базу данными (сиды — налоговые ставки и границы):
   ```bash
   npm run seed
   ```
5. Запустите сервер в режиме разработки:
   ```bash
   npm run dev
   ```
   *Сервер будет доступен по адресу: http://localhost:3001*

### 3. Запуск Фронтенда (Client)
Наконец, запустите интерфейс.
1. Откройте **новое окно терминала** (не закрывая сервер).
2. Перейдите в папку клиента:
   ```bash
   cd client
   ```
3. Установите зависимости:
   ```bash
   npm install
   ```
4. Запустите фронтенд:
   ```bash
   npm run dev
   ```
   *Приложение откроется по адресу: http://localhost:5173 (или другом, указанном в терминале).*

---

### 💡 Полезные советы
- **PostGIS**: Приложение использует географические данные. Если при запуске миграций возникают ошибки, убедитесь, что контейнер Docker полностью поднялся.
- **Порты**:
  - `5432` — База данных
  - `3001` — API (Бэкенд)
  - `5173` — Vite (Фронтенд)
